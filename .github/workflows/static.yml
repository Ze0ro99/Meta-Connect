# دفع التعديلات إلى الفرع الرئيسي
git push origin main
[![GitHub Pages Workflow Status](https://github.com/Ze0ro99/Advanced-social-application-/actions/workflows/select.yml/static.svg)](https://github.com/Ze0ro99/Advanced-social-application-/actions/workflows/static.yml)
name: نشر محتوى ثابت على الصفحات
on:

  push:

    branches:

      - "main"  # يتم تشغيله عند الدفع إلى الفرع الرئيسي

  workflow_dispatch:  # السماح بتشغيل العمل يدويًا من GitHub

permissions:

  contents: read       # إذن قراءة المحتوى

  pages: write         # إذن الكتابة على الصفحات

  id-token: write      # تصريح كتابة رمز المعرف

concurrency:

  group: "pages"   # منع أكثر من نشر واحد في نفس الوقت

  cancel-in-progress: false  # عدم إلغاء عمليات النشر الجارية

    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false


jobs:

  deploy:


    runs-on: ubuntu-latest  # بيئة التشغيل

    runs-on: ubuntu-latest


    environment:

      name: github-pages


      url: ${{ steps.deployment.outputs.page_url }}  # رابط الصفحة بعد النشر

    steps:

      - name: Checkout repository  # جلب الكود من المستودع

        uses: actions/checkout@v4

      - name: Setup Pages  # إعداد الصفحات

        uses: actions/configure-pages@v5

      - name: Upload artifact  # رفع الملفات المطلوبة للنشر


      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1

        with:


          path: '.'  # تحديد المسار لرفع جميع الملفات

      - name: Deploy to GitHub Pages  # نشر الصفحات

          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment

        uses: actions/deploy-pages@v1

      - name: Verify Deployment  # التحقق من نجاح النشر

        run: |

          echo "الصفحة تم نشرها بنجاح على: ${{ steps.deployment.outputs.page_url }}"
name: نشر محتوى ثابت على الصفحات

on:
  push:
    branches: [ "main" ]  # تشغيل العمل عند الدفع إلى الفرع الرئيسي
  workflow_dispatch:  # السماح بتشغيل العمل يدويًا من GitHub

permissions:
  contents: read       # إذن قراءة المحتوى
  pages: write         # إذن الكتابة على الصفحات
  id-token: write      # إذن كتابة رمز المعرف

concurrency:
  group: "pages"  # منع تشغيل أكثر من نشر واحد في نفس الوقت
  cancel-in-progress: false  # عدم إلغاء عمليات النشر الجارية

jobs:
  deploy:
    runs-on: ubuntu-latest  # بيئة التشغيل

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}  # رابط الصفحة بعد النشر

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # جلب الكود من المستودع

      - name: Setup Pages  # إعداد الصفحات
        uses: actions/configure-pages@v5  # إعداد صفحات GitHub

      - name: Upload artifact  # رفع الملفات المطلوبة للنشر
        uses: actions/upload-pages-artifact@v1
        with:
          path: '.'  # تحديد المسار لرفع جميع الملفات

      - name: Deploy to GitHub Pages  # نشر الصفحات
        id: deployment
        uses: actions/deploy-pages@v1
# إضافة الملف إلى Git
git add .github/workflows/static.yml

# إنشاء commit مع رسالة توضيحية
git commit -m "إعداد سير عمل GitHub Pages"

# دفع التعديلات إلى الفرع الرئيسي
git push origin main
[![GitHub Pages Workflow Status](https://github.com/Ze0ro99/Advanced-social-application-/actions/workflows/pages.yml/badge.svg)](https://github.com/Ze0ro99/Advanced-social-application-/actions/workflows/static.yml)

